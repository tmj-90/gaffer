# Build output & deps
node_modules/
**/node_modules/
dist/
**/dist/
build/
coverage/
**/coverage/
*.tsbuildinfo

# Lockfile (machine-generated)
pnpm-lock.yaml

# Markdown — avoid reflowing prose / docs
*.md
**/*.md

# Runner skill manifests (authored prose, not code)
runner/skills/**/SKILL.md

# Local factory state
.gaffer/
.crew/

# P1b golden fixtures — EXACT BYTES captured from the live bash path
# (runner/test/capture-context-golden.sh); formatting them would break the
# byte-parity tests. inputs.json is machine-written by the same harness.
packages/crew/test/fixtures/tick-context/*.golden.json
packages/crew/test/fixtures/tick-context/*.golden.txt
packages/crew/test/fixtures/tick-context/inputs.json
